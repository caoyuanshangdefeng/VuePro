事实上,var的设计可以看成是JavaScript语言设计上的错误,但是这种错误多半不能修复和移除,以为需要像后兼容
    大概10年前,Brendan Eich就决定修复这个问题,于是他添加了一个新的关键字:let
    可以将let看成完美的var
块级作用域
    JS中使用var来声明一个变量时,变量的作用域主要是和函数有关的
    针对于其他块定义来说是没有作用域的,比如if/for等,在开发中往往会引起一些问题

